let arrayNombres = ["Fernando", "German", "Celeste", "Milagros"];


for (let i = 0; i < arrayNombres.length; i++) {
    console.log(`Posición en la lista ${i+1}: ${arrayNombres[i]}`);
    alert(`Posición en la lista ${i+1}: ${arrayNombres[i]}`);
};

